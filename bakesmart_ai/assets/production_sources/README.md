# Production Blender Sources

Store editable `.blend` source files here using the paths declared in `data/production_assets_v1/asset_manifest.csv`.

Each source must contain a root object named `BS_ROOT`, use metre-based true dimensions, node-based Principled PBR materials, and pass the Blender exporter checks before a GLB can be reviewed for production.
